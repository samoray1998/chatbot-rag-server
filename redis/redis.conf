# redis/redis.conf
bind 0.0.0.0
port 6379
timeout 0
tcp-keepalive 300

# Memory management
maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence (fixed syntax)
save 900 1
save 300 10

# Security (uncomment and change password)
requirepass yourstrongpassword